server:
  forward-headers-strategy: FRAMEWORK
spring:
  application:
    name: spring-boot-redis-based-session
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
logging:
  level:
    root: info
  file:
    enabled: false